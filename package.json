{
    "name": "quant-cloud-ecr-action",
    "version": "1.0.0",
    "description": "GitHub Action for generating ECR credentials for Quant Cloud",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc",
        "test": "jest"
    },
    "dependencies": {
        "@actions/core": "^1.10.0",
        "quant-ts-client": "github:quantcdn/quant-ts-client#v3"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "@vercel/ncc": "^0.38.3",
        "typescript": "^5.0.0"
    }
}
